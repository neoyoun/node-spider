这是用vim编辑器编写的一个文本;
基本使用的命令是：
i:切换为输入模式;
ESC:返回为正常模式

正常模式下的常用命令有
方向键：移动光标;
没有方向键的时候
使用 h(左) j(下) k(上) l(右)
win系统下面还是有点问题，譬如输入状态返回，再使用h j k l 移动光标有点问题
看起来跟输入法有关系，
英文键盘下一切正常

每次都要重新设置这个autocrlf=false 是什么鬼
